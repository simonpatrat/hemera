.dropdown {
  position: relative;

  .dropdown__panel {
    display: block;
    opacity: 0;
    transition: all 300ms ease;
    position: absolute;
    top: calc(100% + 20px);
    left: 0;
    width: 100%;
    min-width: 200px;
    padding: 12px 8px;
    border-radius: 5px;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    z-index: 1001;
    box-shadow: 2px 2px 10px 0 rgba(0, 0, 0, 0.4);
    transform: translateY(10px);
    background: var(--dropdown-panel-bg-color);
    color: var(--dropdown-color);

    &::before {
      content: "";
      position: absolute;
      top: -10px;
      right: 0;
      width: 0;
      height: 0;
      border-left: 10px solid transparent;
      border-right: 10px solid transparent;
      border-bottom: 10px solid var(--dropdown-panel-caret-bg-color);
      z-index: 1002;
    }
  }

  &.dropdown--right {
    .dropdown__panel {
      left: auto;
      right: 0;
    }
  }

  &.with-panel-visible {
    .dropdown__panel {
      opacity: 1;
      transform: translateY(0);
    }
  }

  &.transition-ended {
    .dropdown__panel {
      display: none;
    }

    &.with-panel-visible {
      .dropdown__panel {
        display: block;
      }
    }
  }

  .dropdown__list {
    border-top: 1px solid rgba(0, 0, 0, 0.2);
    list-style: none;
    padding: 0;

    &__item {
      width: 100%;
      padding: 12px 8px;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
  }
}
